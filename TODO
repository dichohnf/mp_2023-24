In VideoFrame si deve gestire l'inerimento delle posizione di origine. In composed si può creare un nuovo frame con x e y minimi come origine e massimi come opposto, e inserire i videoframe con le posizioni modificate. Si può chiamare nextFrame() del figlio ???
